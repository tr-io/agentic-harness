{
  "name": "@tr-io/harness",
  "version": "0.1.2",
  "description": "Agentic development harness for Claude Code projects",
  "type": "module",
  "bin": {
    "harness": "./dist/cli.cjs"
  },
  "scripts": {
    "build": "tsup",
    "build:plugin": "npm run build && chmod +x packages/plugin/bin/harness",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "inquirer": "^13.3.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
