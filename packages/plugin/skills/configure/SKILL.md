---
description: Configure harness feature flags (feedforward/feedback features on/off).
---

Run `harness configure $ARGUMENTS` to toggle harness features.
If no $ARGUMENTS, run `harness configure --list` to show current settings.
