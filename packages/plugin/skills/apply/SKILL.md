---
description: Sync disk state to match harness configuration (re-scaffold changed features).
---

Run `harness apply` to apply the current harness configuration to disk.
Report which files were added, updated, or removed.
