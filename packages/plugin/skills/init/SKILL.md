---
description: Scaffold the agentic harness in the current project (CLAUDE.md, .ai/, hooks). Run once per repo.
---

Run `harness init` in the current working directory to scaffold the agentic harness.
If $ARGUMENTS is provided, pass it as flags (e.g. `--force`).
Report the list of files created and any next steps.
